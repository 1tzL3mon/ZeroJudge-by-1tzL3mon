#include <iostream>
#include <vector>
using namespace std;

void MergeSort(vector<int> &a, int left, int right) {
    if (right - left == 1) return;
    int mid = left + (right-left) / 2;

    MergeSorta, left, mid);

    MergeSort(a, mid, right);

    vector<int> buf;
    for (int i = left ; i < mid; ++1) buf.push_back(a[i]);
    for (int i = right - 1; i >= mid; --i) buf.push_back(a[i]);

    int
